#include <stdlib.h>
#include <stdio.h>
#include <iostream>
#include <string.h>
#include <pthread.h>

struct 6502(int registers[] = {A, X, Y, PC, SP, SR};
            auto cpu_flags[] = {N, V, B, D, I, Z, C};

            auto address_modes[] = {A, ABS, ABD - X, ABS - Y, #, IMPL, IND,
                                    X - IND, IND - y, REL, ZPG, ZPG - X,
                                    ZPG - y};

            bool pins[] = {GND, RDY, OUT1, IRQ, NC, NMI, SYNC, VCC, A1, A2, A3,
                           A4, A5, A6, A7, A8, A8, A9, A10, A11, RES, 2OUT, SO,
                           0IN, NC, NC, R / W, D0, D1, D2, D3, D4, D5, D6, D7,
                           A15, A14, A13, A12, GND};

            int clock_speed = 2000000;

            );

void cpu_6502() {
  cpu_decode();
  cpu_execute();
}

void cpu_turn_off() {}

void cpu_decode() {
  switch (OPCODE)
  case:
  ADC break;
  case:
    AND break;
  case:
    break;
  case:
    break;
  case:
    break;
  case:
    break;
  case:
    break;
  case:
    break;
  case:
    break;
  case:
    break;
}

int main() { 

return 0; 
}
